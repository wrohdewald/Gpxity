#!/bin/bash

set -e

parentdir=$(basename $(dirname $(pwd -P)))
currentdir=$(basename $(pwd -P))

test "$parentdir/$currentdir" != Gpxmove/doc && (
	echo you must be in Gpxmove/doc
	echo you are in "$parentdir/$currentdir"
	exit 2
)

sphinx-apidoc -f -o source  ../gpxmove 

make html
